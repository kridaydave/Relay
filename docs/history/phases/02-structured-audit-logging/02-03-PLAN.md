---
phase: 02-structured-audit-logging
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/relay/core_pipeline.py
  - tests/unit/test_audit_events.py
autonomous: true
requirements: [AUD-01]
user_setup: []
must_haves:
  truths:
    - "Pipeline emits fork_started event when parallel execution begins"
    - "Pipeline emits fork_completed event after forks complete"
    - "Pipeline emits join_completed event after merge strategy applied"
    - "All parallel events carry fork_count, forks_succeeded, and join_strategy fields"
  artifacts:
    - path: "src/relay/core_pipeline.py"
      provides: "Parallel audit event emission in execute_parallel_step"
      contains: "ForkCompleted"
  key_links:
    - from: "execute_parallel_step"
      to: "self._emit_audit_event"
      via: "fork_started before fork_coros construction, fork_completed after gather, join_completed after merge"
---

<objective>
Wire 3 parallel execution audit events in `execute_parallel_step`: `fork_started`, `fork_completed`, and `join_completed`. These events make pipeline fork/join operations observable, with fork count, success count, and join strategy metadata.

Purpose: After this plan, all parallel execution operations emit structured audit events. Operators can observe how many forks ran, how many succeeded, and which join strategy was applied.
Output: 3 audit events emitted from execute_parallel_step.
</objective>

<execution_context>
@C:/Users/NewAdmin/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/NewAdmin/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/history/02-structured-audit-logging/02-CONTEXT.md
@docs/history/02-structured-audit-logging/02-RESEARCH.md

<canonical_refs>
From CONTEXT.md Integration Points table (lines 689-717):
- Event 12 (fork_started): core_pipeline.execute_parallel_step ~line 558 before fork
- Event 13 (fork_completed): core_pipeline.execute_parallel_step after gather ~line 582
- Event 14 (join_completed): core_pipeline.execute_parallel_step after apply_join_strategy ~line 584
</canonical_refs>

<interfaces>
From src/relay/core_pipeline.py execute_parallel_step (line 504):

```python
async def execute_parallel_step(
    self,
    fork_specs: list[ForkSpec],
    join_strategy: JoinStrategy,
) -> Result[ContextEnvelope]:
    # Line 532-556: input validation + slice/budget
    # Line 558: parallel_id = str(uuid.uuid4())
    # Line 559-569: fork_coros construction
    # Line 571-584: join_strategy dispatch
    #   - FIRST_WINS: apply_join_strategy with first_wins_coros
    #   - UNION/VOTE: asyncio.gather(*fork_coros) then apply_join_strategy
    # Line 586-587: if merged_result is Failure, return
    # Line 589: combined hash
    # Line 595-644: transaction block for merge + sign + validate + snapshot
```

Events to import:
```python
from relay.audit import ForkStarted, ForkCompleted, JoinCompleted
```

Event fields:
- ForkStarted(pipeline_id, step, fork_count)
- ForkCompleted(pipeline_id, step, forks_succeeded)
- JoinCompleted(pipeline_id, step, join_strategy)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit fork_started, fork_completed, and join_completed events in execute_parallel_step</name>
  <files>
    src/relay/core_pipeline.py
  </files>
  <read_first>
    src/relay/core_pipeline.py — execute_parallel_step (lines 504-644). Key areas: validation block (532-556), fork_coros construction (559-569), join dispatch (571-584), Failure return (586-587), merge+transaction block (595-644)
    src/relay/audit/events.py — ForkStarted, ForkCompleted, JoinCompleted event field definitions
    src/relay/envelope.py — ContextEnvelope.fork_count and forks_succeeded fields
  </read_first>
  <action>
    Modify `core_pipeline.py`. Add imports:
    ```python
    from relay.audit import ForkCompleted, ForkStarted, JoinCompleted
    ```

    **In `execute_parallel_step` (line 504):**

    1. **`fork_started`**: After input validation passes and before `fork_coros` construction (after line 556's budget check, before line 558 `parallel_id = ...`). Emit:
       ```python
       self._emit_audit_event(ForkStarted(
           pipeline_id=self._pipeline_id,
           step=pre_fork_envelope.step,
           fork_count=len(fork_specs),
       ))
       ```
       Important: pre_fork_envelope is available at this point — it's bound from the `with self._state.transaction() as pre_fork_envelope:` block (line 543). The fork_started emit must happen INSIDE that transaction block (before it exits at line 557).

    2. **`fork_completed`**: After forks complete and BEFORE merge strategy. For FIRST_WINS path (line 571-580): emit after `await apply_join_strategy(...)` returns and after checking it's Success (line 580 area). For UNION/VOTE path (line 582-584): emit after `asyncio.gather(*fork_coros)` returns (line 582) and BEFORE `await apply_join_strategy(...)`. 
       
       Actually, the fork_completed event should capture `forks_succeeded` which is only available after the fork results are collected. Let me place carefully:
       
       For UNION/VOTE (line 582-584):
       - Line 582: `collected = list(await asyncio.gather(*fork_coros))`
       - Line 583: `forks_succeeded = sum(1 for r in collected if r.success)`
       - After line 583, before line 584: emit `ForkCompleted(pipeline_id=self._pipeline_id, step=pre_fork_envelope.step, forks_succeeded=forks_succeeded)`
       
       For FIRST_WINS (line 571-580):
       - Line 572-579: `merged_result = await apply_join_strategy(...)`
       - Line 580: `forks_succeeded = 1 if isinstance(merged_result, Success) else 0`
       - After line 580, before the merge check: emit `ForkCompleted(pipeline_id=self._pipeline_id, step=pre_fork_envelope.step, forks_succeeded=forks_succeeded)`

    3. **`join_completed`**: After merge strategy is applied successfully, before returning Failure from merged_result (line 586-587). If merge fails, don't emit join_completed (the join didn't complete). 
       
       After line 587's Failure check passes (i.e., merged_result IS a Success), before the transaction block (line 595): emit `JoinCompleted(pipeline_id=self._pipeline_id, step=pre_fork_envelope.step, join_strategy=join_strategy.value)`

    **Variable availability check:**
    - `pre_fork_envelope` is accessible from the `with self._state.transaction()` block (lines 543-557). It's also accessible after that block since `pre_fork_envelope` was captured from the context manager expression. Confirmed by reading the Python code: the variable `pre_fork_envelope` is bound during the `with` statement and remains accessible after.
    - `fork_specs` is a parameter — always available.
    - `join_strategy` is a parameter — always available.
    - `forks_succeeded` is computed at lines 580 or 583.

    **Important — step number:** The step number is `pre_fork_envelope.step` because in `execute_parallel_step`, the new envelope hasn't been created yet. The current envelope's step is the pre-fork step. The fork creates an envelope at `pre_fork_envelope.step + 1`, but that's created later in the transaction block (line 602-609). Use `pre_fork_envelope.step` for all three events since they occur before the new envelope is created.
  </action>
  <verify>
    <automated>python -c "
import ast
with open('src/relay/core_pipeline.py') as f:
    tree = ast.parse(f.read())
emit_calls = [n for n in ast.walk(tree) if isinstance(n, ast.Expr) and isinstance(n.value, ast.Call) and hasattr(n.value.func, 'attr') and n.value.func.attr == '_emit_audit_event']
event_types = set()
for call in emit_calls:
    if call.value.args and hasattr(call.value.args[0], 'func') and hasattr(call.value.args[0].func, 'attr'):
        event_types.add(call.value.args[0].func.attr)
print('Events:', sorted(event_types))
assert 'ForkStarted' in event_types
assert 'ForkCompleted' in event_types
assert 'JoinCompleted' in event_types
print('All parallel events present')
"
  </verify>
  <done>
    All 3 parallel audit events emitted from execute_parallel_step with correct metadata
  </done>
  <acceptance_criteria>
    - grep -n "_emit_audit_event(ForkStarted" src/relay/core_pipeline.py returns 1 match
    - grep -n "_emit_audit_event(ForkCompleted" src/relay/core_pipeline.py returns 2 matches (FIRST_WINS and UNION/VOTE paths)
    - grep -n "_emit_audit_event(JoinCompleted" src/relay/core_pipeline.py returns 1 match
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Integration tests for parallel audit events</name>
  <files>
    tests/unit/test_audit_events.py
  </files>
  <read_first>
    src/relay/core_pipeline.py — execute_parallel_step (after Task 1 changes)
    src/relay/audit/events.py — ForkStarted, ForkCompleted, JoinCompleted field definitions
    tests/conftest.py — FixedAuditSink
  </read_first>
  <action>
    Add test class `TestParallelAuditEvents` to `tests/unit/test_audit_events.py`:

    1. `test_fork_started_event_has_fork_count` — construct ForkStarted(pipeline_id="p", fork_count=3), assert event_type == "fork_started", assert fork_count == 3
    2. `test_fork_completed_event_has_forks_succeeded` — construct ForkCompleted(pipeline_id="p", forks_succeeded=2), assert forks_succeeded == 2
    3. `test_join_completed_event_has_join_strategy` — construct JoinCompleted(pipeline_id="p", join_strategy="first_wins"), assert join_strategy == "first_wins"
    4. `test_parallel_events_are_frozen` — verify each parallel event type is immutable (pytest.raises(AttributeError) on mutation)

    Test naming: sentences per AGENTS.md Rule 7.1.
  </action>
  <verify>
    <automated>pytest tests/unit/test_audit_events.py -v -k "parallel or fork or join" --tb=short</automated>
  </verify>
  <done>
    All parallel event tests pass
  </done>
  <acceptance_criteria>
    pytest tests/unit/test_audit_events.py -v -k "fork or join" -x passes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pipeline → AuditSink | Parallel execution events carry aggregate metadata (fork_count, forks_succeeded) — never individual fork payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-P03-01 | Information Disclosure | Fork events | mitigate | Fork events carry only aggregate counts (fork_count, forks_succeeded) and join_strategy — never per-fork payload content. PayloadRedactor not needed because events carry no payload fields. |
| T-02-P03-02 | Tampering | ForkCompleted.forks_succeeded | accept | forks_succeeded is computed from the actual fork results (ForkResult.success). If a fork reports false success, that's a fork runner issue, not an audit issue. |
</threat_model>

<verification>
```bash
pytest tests/unit/test_audit_events.py -v -k "fork or join or parallel"
python -m mypy --strict src/relay/core_pipeline.py
```
</verification>

<success_criteria>
1. fork_started emitted with fork_count from execute_parallel_step
2. fork_completed emitted with forks_succeeded from both FIRST_WINS and UNION/VOTE paths
3. join_completed emitted with join_strategy after successful merge
4. All 3 event construction tests pass
5. mypy --strict zero suppressions on core_pipeline.py
</success_criteria>

<output>
Create `docs/history/02-structured-audit-logging/02-03-SUMMARY.md` when done
</output>
